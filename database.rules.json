{
  "rules": {
    ".read": false,
    ".write": false,

    "commuters": {
      "$uid": {
        ".read": "auth != null && auth.uid == $uid",
        ".write": "auth != null && auth.uid == $uid"
      }
    },

    "drivers": {
      "$uid": {
        ".read": "auth != null && auth.uid == $uid",
        ".write": false
      }
    },

    "emergencies": {
      "$emergencyId": {
        ".read": "auth != null",
        ".write": "auth != null",
        ".validate": "newData.hasChildren(['driverId','type','latitude','longitude','status','timestamp']) && newData.child('driverId').isString() && newData.child('driverId').val() === auth.uid && newData.child('type').isString() && newData.child('type').val().length > 0 && newData.child('latitude').isNumber() && newData.child('latitude').val() >= -90 && newData.child('latitude').val() <= 90 && newData.child('longitude').isNumber() && newData.child('longitude').val() >= -180 && newData.child('longitude').val() <= 180 && newData.child('status').isString() && (newData.child('status').val() === 'pending' || newData.child('status').val() === 'acknowledged' || newData.child('status').val() === 'resolved') && newData.child('timestamp').isNumber()"
      }
    }
  }
}
